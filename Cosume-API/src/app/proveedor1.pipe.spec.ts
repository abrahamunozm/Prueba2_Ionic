import { Proveedor1Pipe } from './proveedor1.pipe';

describe('Proveedor1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Proveedor1Pipe();
    expect(pipe).toBeTruthy();
  });
});
